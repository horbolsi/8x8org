#!/usr/bin/env bash
target="/home/runner/workspace/repos/8x8org"
echo "→ 8xr: entering $target  (type: exit  to return)"
cd "$target" || exit 1
export PS1="(8xr) ${PS1:-\u@\h:\w\$ }"
exec "${SHELL:-/bin/bash}" -i
