#!/usr/bin/env bash
target="/home/runner/workspace"
echo "→ 8x: entering $target  (type: exit  to return)"
cd "$target" || exit 1
export PS1="(8x) ${PS1:-\u@\h:\w\$ }"
exec "${SHELL:-/bin/bash}" -i
